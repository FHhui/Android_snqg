<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hp.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HelloActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".submit_Activity" />
        <activity android:name=".personinfo_activity" />
        <activity android:name=".activity_test" />
        <activity android:name=".activity_redemtion" />
        <activity android:name=".activity_study_online" />
        <activity android:name=".activity_watchTV" />
        <activity android:name=".activity_helloinfo" />
        <activity android:name=".activity_testresult"></activity>
    </application>

</manifest>